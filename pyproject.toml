[project]
name = "tsm-dataanalyse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

[tool.uv]
exclude-newer = "1 week"

dependencies = [
    "dbt-bigquery>=1.11, <1.12",
    "dbt-core>=1.11, <1.12",
    "google-cloud-bigquery>=3.40, <4.0",
]

[dependency-groups]
lint = [
    "pre-commit>=4.5.1",
]
