[project]
name = "tsm-dataanalyse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.12, <1.13",
    "dbt-core==1.12.3",
    "google-cloud-bigquery>=3.45.0, <4.0",
    "plotly==7.0.0",
    "polars==1.44.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
lint = [
    "pre-commit==4.6.2",
]
[tool.uv]
exclude-dependencies = [
    "dbt-core-experimental-parser"
]
